---
# Parent app that discovers all security infrastructure apps
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-security
  namespace: argocd
spec:
  project: infra
  source:
    repoURL: https://github.com/guilhermewolf/atoca.house
    targetRevision: HEAD
    path: infra/k8s/security
    directory:
      recurse: true
      include: '*/application.yaml'
      exclude: 'application.yaml'
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
