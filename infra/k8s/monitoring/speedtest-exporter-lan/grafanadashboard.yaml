---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: speedtest-lan-dashboard
spec:
  allowCrossNamespaceImport: true
  # Unique UID: LAN and VPN import the same upstream dashboard (20115), which
  # carries a single uid. Without an override the second import would relocate
  # the first instead of creating a separate dashboard.
  uid: speedtest-exporter-lan
  datasources:
    - datasourceName: prometheus
      inputName: DS_PROMETHEUS
  instanceSelector:
    matchLabels:
      grafana.internal/instance: grafana
  url: https://grafana.com/api/dashboards/20115/revisions/4/download
