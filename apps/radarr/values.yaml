controllers:
  radarr:
    annotations:
      reloader.stakater.com/auto: "true"
    containers:
      radarr:
        image:
          repository: lscr.io/linuxserver/radarr
          tag: 5.10.4
        env:
          TZ: Europe/Amsterdam
service:
  radarr:
    controller: radarr
    ports:
      http:
        port: 7878

ingress:
  radarr:
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: "cloudflare-prod"
    hosts:
      - host: radarr.atoca.house
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: radarr
              port: http
    tls:
      - hosts:
          - radarr.atoca.house
        secretName: radarr-tls

persistence:
  media:
    type: nfs
    server: nas.atoca.house
    path: /mnt/user/media-files/movies/
    globalMounts:
      - path: /movies

  media-downloads:
    type: nfs
    server: nas.atoca.house
    path: /mnt/user/media-downloads/complete
    globalMounts:
      - path: /downloads/complete


  config:
    enabled: true
    type: persistentVolumeClaim
    storageClass: nfs-client
    accessMode: ReadWriteOnce
    size: 1Gi
    globalMounts:
      - path: /config

resources:
  limits:
    cpu: "1"
    memory: "1Gi"

replicaCount: 1

nodeSelector: {}
tolerations: []
affinity: {}
