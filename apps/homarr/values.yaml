ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "cloudflare-prod"
  hosts:
    - host: home.atoca.house
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - "home.atoca.house"
      secretName: homarr-tls