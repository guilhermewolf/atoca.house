---
controllers:
  main:
    annotations:
      reloader.stakater.com/auto: "true"
    containers:
      main:
        image:
          # renovate: datasource=docker depName=ghcr.io/codewithcj/sparkyfitness-frontend
          repository: ghcr.io/codewithcj/sparkyfitness-frontend
          tag: v0.16.4.5@sha256-a35c449a22484ac67a65bd2b5e2c5e9523360975671aa72da06d8561b936b27e
        env:
          PROMETHEUS_ENABLED: "true"
          SPARKY_FITNESS_LOG_LEVEL: info
          SPARKY_FITNESS_SERVER_HOST: sparkyfitness-server.sparkfitness.svc.cluster.local
          SPARKY_FITNESS_SERVER_PORT: 3010
        resources:
          requests:
            cpu: 15m
            memory: 64M
          limits:
            memory: 64M
service:
  main:
    controller: main
    ports:
      http:
        port: &httpPort 80

route:
  app:
    enabled: true
    kind: HTTPRoute
    parentRefs:
      - name: atoca-house-internal
        namespace: kube-system
    hostnames:
      - fitness.atoca.house
    rules:
      - backendRefs:
          - name: sparkyfitness-frontend
            port: *httpPort
        matches:
          - path:
              type: PathPrefix
              value: /
