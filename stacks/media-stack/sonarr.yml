version: "2.1"
services:
  sonarr:
    image: ghcr.io/linuxserver/sonarr
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Amsterdam
      - UMASK_SET=022
    volumes:
      - sonarr:/config
      - /volume1/Media/downloads:/downloads
      - /volume1/Media/series:/media
    ports:
      - 8989:8989
    restart: always
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

volumes:
  sonarr:
