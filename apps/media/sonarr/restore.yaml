---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: sonarr-restore
  namespace: sonarr
spec:
  trigger:
    manual: restore-once
  restic:
    repository: sonarr-r2-secret
    copyMethod: Direct
    destinationPVC: sonarr
    storageClassName: ceph-block
    volumeSnapshotClassName: csi-ceph-blockpool
    moverSecurityContext:
      runAsUser: 99
      runAsGroup: 100
      fsGroup: 100
