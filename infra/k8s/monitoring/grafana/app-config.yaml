---
# Application configuration for Grafana Operator
app:
  name: grafana
  namespace: grafana
  chart: grafana-operator
  chartRepo: ghcr.io/grafana/helm-charts
  chartVersion: 5.25.0
  pruneEnabled: false # Grafana operator should not auto-prune
  useVaultPlugin: false
