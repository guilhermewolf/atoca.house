apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homarr

resources:
  - namespace.yaml

helmCharts:
  - name: homarr
    repo: https://oben01.github.io/charts/
    releaseName: "homarr"
    version: 0.15.3
    valuesFile: values.yaml
