---
# Ansible Galaxy requirements for roles and collections

collections:
  # Ansible POSIX collection for enhanced functionality
  - name: ansible.posix
    version: ">=1.5.0"

  # Kubernetes collection for k8s module
  - name: kubernetes.core
    version: ">=2.4.0"

  # Community general collection
  - name: community.general
    version: ">=7.0.0"

# External roles (if needed in the future)
roles: []
