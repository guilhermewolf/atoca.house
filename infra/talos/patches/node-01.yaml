---
# Node-specific configuration for node-01 (192.168.178.201)

machine:
  network:
    hostname: node-01
    interfaces:
      - interface: eth0  # Adjust to your actual interface name (check with: talosctl get links)
        dhcp: false
        addresses:
          - 192.168.178.201/24
        routes:
          - network: 0.0.0.0/0
            gateway: 192.168.178.1
