apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: znc
  namespace: argocd
spec:
  destination:
    namespace: znc
    server: https://kubernetes.default.svc
  project: applications
  sources:
    - chart: app-template
      repoURL: ghcr.io/bjw-s-labs/helm
      targetRevision: 4.5.0
      helm:
        releaseName: znc
        valueFiles:
        - $values/apps/communication/znc/values.yaml
    - repoURL: 'https://github.com/guilhermewolf/atoca.house'
      targetRevision: HEAD
      ref: values
    - repoURL: 'https://github.com/guilhermewolf/atoca.house'
      targetRevision: HEAD
      path: apps/communication/znc/
      directory:
        exclude: '{application.yaml,values.yaml}'
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
