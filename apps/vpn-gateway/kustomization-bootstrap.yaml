apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vpn-gateway

# Bootstrap resources - deploy these first before Cilium is updated
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - external-secret.yaml
  - pvc.yaml
  # Cilium policies will be added after Cilium is updated:
  # - cilium-egress-gateway.yaml
  # - network-policies.yaml
