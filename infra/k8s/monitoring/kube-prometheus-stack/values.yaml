crds:
  enabled: true

nameOverride: "monitoring"
cleanPrometheusOperatorObjectNames: true

alertmanager:
  route:
    main:
      enabled: true
      hostnames:
        - alertmanager.atoca.house
      parentRefs:
        - name: atoca-house-internal
          namespace: kube-system
  alertmanagerSpec:
    cleanPrometheusOperatorObjectNames: true
    alertmanagerConfiguration:
      name: alertmanager
    externalUrl: https://alertmanager.atoca.house
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: ceph-block
          resources:
            requests:
              storage: 1Gi
  ingress:
    enabled: false

kubeEtcd:
  service:
    selector:
      component: kube-apiserver

prometheus:
  route:
    main:
      enabled: true
      hostnames:
        - prometheus.atoca.house
      parentRefs:
        - name: atoca-house-internal
          namespace: kube-system
  prometheusSpec:
    externalUrl: https://prometheus.atoca.house
    # RSS sits at ~2.6Gi, so a 3000Mi limit left only ~550Mi of page cache for a
    # 19.5GB TSDB on ceph-block. Every rule evaluation round therefore re-read
    # chunks from Ceph over the network (~176MB/s sustained, 2M samples/s), which
    # is what starved etcd raft on the shared bond0. 6Gi leaves ~3.4Gi of cache,
    # enough to hold the recent blocks the ruleset actually touches.
    # Keep requests == limits so the pod stays Guaranteed QoS.
    resources:
      requests:
        cpu: 500m
        memory: 6Gi
      limits:
        memory: 6Gi
    podMonitorSelectorNilUsesHelmValues: false
    probeSelectorNilUsesHelmValues: false
    ruleSelectorNilUsesHelmValues: false
    scrapeConfigSelectorNilUsesHelmValues: false
    serviceMonitorSelectorNilUsesHelmValues: false
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: ceph-block
          resources:
            requests:
              storage: 100Gi
    # TSDB lives on ceph-block, so every compaction pass reads blocks back over
    # the network from all 4 OSDs and converges on whichever node runs this pod.
    # 14d/727k series was ~34GB on disk and drove ~113MB/s of cluster-wide reads,
    # which starved etcd raft on the shared bond0. 7d halves block size and read
    # volume. retentionSize is a safety net below the PVC, not the binding limit.
    retention: 7d
    retentionSize: "20GB"
    walCompression: true
  ingress:
    enabled: false

prometheus-node-exporter:
  fullnameOverride: node-exporter
  prometheus:
    monitor:
      enabled: true
      relabelings:
    relabelings:
      - action: replace
        regex: (.*)
        replacement: $1
        sourceLabels:
          - __meta_kubernetes_pod_node_name
        targetLabel: kubernetes_node
      - action: replace
        regex: (.*)
        replacement: $1
        sourceLabels:
          - __meta_kubernetes_pod_node_name
        targetLabel: nodename
      - action: replace
        regex: (.*)
        replacement: $1.atoca.house:9100
        sourceLabels:
          - kubernetes_node
        targetLabel: instance

kube-state-metrics:
  fullnameOverride: kube-state-metrics
  metricLabelsAllowlist:
    - pods=[*]
    - deployments=[*]
    - persistentvolumeclaims=[*]
  prometheus:
    monitor:
      enabled: true
      relabelings:
        - action: replace
          regex: (.*)
          replacement: $1
          sourceLabels: ["__meta_kubernetes_pod_node_name"]
          targetLabel: kubernetes_node

additionalPrometheusRulesMap:
  dockerhub-rules:
    groups:
      - name: dockerhub
        rules:
          - alert: DockerhubRateLimitRisk
            annotations:
              summary: There are {{ $value }} containers pulling from Dockerhub. This may lead to rate limiting.
            expr: count(time() - container_last_seen{image=~"(docker.io).*",container!=""} < 30) > 100
            labels:
              severity: critical
  oom-rules:
    groups:
      - name: oom
        rules:
          - alert: OomKilled
            annotations:
              summary: Container {{ $labels.container }} in pod {{ $labels.namespace }}/{{ $labels.pod }} has been OOMKilled {{ $value }} times in the last 10 minutes.
            expr: (kube_pod_container_status_restarts_total - kube_pod_container_status_restarts_total offset 10m >= 1) and ignoring (reason) min_over_time(kube_pod_container_status_last_terminated_reason{reason="OOMKilled"}[10m]) == 1
            labels:
              severity: critical

  zfs-rules:
    groups:
      - name: zfs
        rules:
          - alert: ZfsUnexpectedPoolState
            annotations:
              summary: ZFS pool {{$labels.zpool}} on {{$labels.instance}} is in a unexpected state {{$labels.state}}
            expr: node_zfs_zpool_state{state!="online"} > 0
            labels:
              severity: critical
kubeProxy:
  enabled: false

grafana:
  enabled: false
  forceDeployDashboards: true
