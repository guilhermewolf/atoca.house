apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freqtrade-pvc
  namespace: freqtrade
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-hostpath 
  resources:
    requests:
      storage: 1Gi
