---
# Node-specific configuration for node-01 (192.168.40.11)

machine:
  network:
    interfaces:
      - interface: enp5s0f3u2  # Adjust to your actual interface name (check with: talosctl get links)
        dhcp: true
        vlans:
          - vlanId: 6  # VPN VLAN (for Kubernetes workloads)
            dhcp: false
            mtu: 1500
